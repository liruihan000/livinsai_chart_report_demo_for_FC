import { describe, it, expect } from 'vitest'

describe('PdfPanel', () => {
  it.todo('renders PDF viewer when pdfUrl provided')
  it.todo('shows loading state when pdfUrl is null')
  it.todo('calls onClose when close button clicked')
  it.todo('calls onDownload when download button clicked')
})
