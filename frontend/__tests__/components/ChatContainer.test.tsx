import { describe, it, expect } from 'vitest'

describe('ChatContainer', () => {
  it.todo('renders empty state when no messages')
  it.todo('renders message list with messages')
  it.todo('shows streaming dots when loading')
  it.todo('disables input when loading')
})
