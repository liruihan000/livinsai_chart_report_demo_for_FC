import { describe, it, expect } from 'vitest'

describe('useChat', () => {
  it.todo('initializes with empty messages')
  it.todo('sends message and appends response')
  it.todo('sets isLoading during request')
  it.todo('handles API errors gracefully')
  it.todo('clears history and generates new session ID')
  it.todo('sets files when response contains file refs')
})
