import { describe, it, expect } from 'vitest'

describe('usePdf', () => {
  it.todo('starts with isOpen false and null pdfUrl')
  it.todo('opens PDF and sets blob URL')
  it.todo('revokes previous blob URL on new open')
  it.todo('closes panel without revoking URL')
  it.todo('triggers download via anchor element')
})
