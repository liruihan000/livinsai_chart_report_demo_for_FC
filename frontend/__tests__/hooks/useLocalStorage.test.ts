import { describe, it, expect } from 'vitest'

describe('useLocalStorage', () => {
  it.todo('returns initial value when key not in storage')
  it.todo('reads existing value from localStorage')
  it.todo('writes updated value to localStorage')
  it.todo('handles JSON parse errors gracefully')
})
